@page "/inicio"
@using Interfaz.ServiciosInterfaz
@using Servicios.Gestores
@inject GestorProyectos GestorProyectos
@rendermode InteractiveServer
@inject LogicaSesion Sesion
@inject NavigationManager Navigation

@if(Sesion.UsuarioLogueado == null || !sesionCargada)
{
}
else
{
    <div class="container mt-5">
        <div class="text-center mb-4">
            <h1 class="display-4 titulo-bienvenida">Bienvenido, @Sesion.UsuarioLogueado.Nombre @Sesion.UsuarioLogueado.Apellido a <strong class="text-dark">TaskTrackPro</strong></h1>
            <h3 class="lead subtitulo">Tu solución integral para la gestión de proyectos.</h3>
        </div>

        <hr class="my-4 separador" />

        <div class="descripcion">
            <p class="mb-4">
                <strong>TaskTrackPro</strong> es una aplicación web diseñada para facilitar la organización, seguimiento y planificación de proyectos en equipo. Permite crear y administrar proyectos, asignar tareas con dependencias, establecer fechas clave y calcular automáticamente el camino crítico para optimizar la duración del proyecto.
            </p>

            <p class="mb-4">
                Con un enfoque colaborativo, <strong>TaskTrackPro</strong> ofrece funcionalidades como notificaciones automáticas, control de permisos por roles y visualización clara del progreso del proyecto.
            </p>

            <p class="mb-4">
                Ideal para equipos que buscan eficiencia, transparencia y control total en la ejecución de sus proyectos.
            </p>
        </div>
    </div>
    
    /*<style>
    body {
        background-color: #f5f1ed;
    }

    .titulo-bienvenida {
        color: #252323;
    }

    .subtitulo {
        color: #70798c;
    }

    .separador {
        border-top: 2px solid #dad2bc;
        width: 60%;
        margin: auto;
    }

    .descripcion {
        background-color: #dad2bc;
        border-radius: 1rem;
        padding: 2rem;
        color: #252323;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
    }

    strong {
        color: #a99985;
    }

    p {
        font-size: 1.1rem;
    }
</style>*/
    
}

@code {
    private bool sesionCargada = false;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender && !sesionCargada)
        {
            bool haySesionActiva = await Sesion.HaySesionActiva();

            if (!haySesionActiva)
            {
                Navigation.NavigateTo("/");
            }
            else
            {
                await Sesion.ActualizarSesion();
                sesionCargada = true;
                StateHasChanged(); // Vuelve a renderizar para mostrar la sesión activa
            }
        }
    }
}